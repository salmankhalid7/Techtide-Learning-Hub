/**
 * @file progress.constants.js
 * @description Constants for the Learning Progress module.
 */

export const PROGRESS_LIMITS = Object.freeze({
    MIN_PERCENTAGE: 0,
    MAX_PERCENTAGE: 100,
});

export const DEFAULT_PROGRESS = Object.freeze({
    COMPLETION_PERCENTAGE: 0,
    TOTAL_TIME_SPENT: 0,
});
